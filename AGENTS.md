# 托业试题集 · 工作指令

这是一个托业（TOEIC）试题与知识整理仓库。用户提供一道题后，把完整题目和详细题解加入试题集，同时把可复用的语法、词汇、固定搭配等知识点分类沉淀进 `notes/` 下的独立笔记，供日后练习和复习。

## 触发条件

仅当用户**粘贴了一道托业题**（题干、选项，文字或截图均可，可能附正确答案）时，才执行下面的写入工作流。

没粘题时不要主动改任何文件——用户可能只是提问、讨论，或让你查阅已有笔记，此时正常回答即可。

## 工作流

**1. 先读后写**
动手前先读 `notes/` 里相关文件，掌握已有条目和它们的格式。新增沿用该文件既有格式；某个词 / 搭配 / 语法点已经记过，就在原条目上补例句或补角度，不要另起一条重复。

**2. 加入试题集并详细拆题**
把完整题目加入 `notes/01_试题集.md`，在标题下以 `**添加日期**：YYYY-MM-DD` 记录题目加入试题集的当天日期，并给出中文译文（填空题代入正确答案译整句）和自包含的详细题解。读者只看这一道题的条目，就应能学到题干中所有有复用价值的知识，不需要跳转到其他笔记。

每道题的解析至少包括：
- **核心考点**：空格或设问直接考什么，以及快速定位答案的信号。
- **句子结构**：拆出主语、谓语、宾语 / 表语，并说明从句、非谓语、介词短语、同位语等修饰关系。
- **语法知识**：系统说明题中出现的相关规则、结构公式、词性位置、时态语态、主谓一致等；不只解释正确答案所在的局部。
- **词汇与固定搭配**：列出题干中值得掌握的单词、词性、含义、短语和搭配。
- **选项辨析**：正确项为何对，每个干扰项为何错；必要时说明词形、语义和使用条件。
- **解题提示**：总结这类题的判断顺序、易错点或可迁移方法。

选项是词 / 短语时不做机械式逐词翻译。听力题（Part 1–4）额外补：连读 / 弱读等语音现象、题干与音频的同义替换、场景信号词。阅读题（Part 6–7）还要补篇章逻辑、指代关系、同义替换和定位依据。

**3. 归类**
把拆出来的知识点放进对应分类文件（见下方分工）。一道题常同时涉及多个维度，可同时写入固定搭配、高频词汇、语法点等文件。

分类笔记是独立知识总结，不与具体试题建立关联：不要写试题编号、出处编号、“关联笔记”或回链。可以保留题中原句作为例证，但只围绕知识点本身总结。

**4. 写入**
`notes/` 下所有笔记都使用追加方式：新增试题、新增词汇、新增搭配、新增语法点等一律加在对应文件最后，保持前面已有条目的顺序。每道新题都加入试题集；分类笔记中已存在的词 / 搭配 / 语法点只在原条目补充新角度，不重复建条。用户给出的选项频率不写入笔记。

**5. 重生成查看器**
在线版 `index.html` 是手维护的静态文件，运行时直接读 `notes/*.md`，**只改笔记内容时什么都不用做**，GitHub Pages 推上去即生效。**新增 / 删除 / 改名笔记文件**时要做两件事：① 手动更新 `index.html` 顶部 `NOTE_FILES` 清单（保持文件顺序）；② 运行 `python3 build.py`（或 `python build.py`）刷新离线双击版 `offline.html`。没有 Python 跑不了就跳过 ②，并在回报里提示 `offline.html` 待刷新。

**6. 回报**
用 3–5 行说明更新了哪些文件、各自加了什么。不要复述原题。

## 文件分工

| 文件 | 装什么 |
|------|--------|
| `notes/01_试题集.md` | 完整题目存档 + 详细题解 |
| `notes/02_高频词汇.md` | 单词的词性、释义、题中例句、易混词提示 |
| `notes/03_固定搭配.md` | 动词与介词搭配、固定短语 |
| `notes/04_语法点.md` | Part 5/6 语法考点，规则 + 例证 |
| `notes/05_长难句.md` | Part 7 长难句的结构拆解 + 翻译 |
| `notes/06_商务场景表达.md` | 邮件 / 会议 / 合同等商务场景表达 |
| `notes/07_易混词辨析.md` | 形近、义近词辨析 |

分类不够用时，新增文件、编号顺延（08、09……），并在 `README.md` 文件表里登记。

## 写作约束

- 中文讲解；单词、例句保留英文原文。
- 例句一律取自题目本身，不自己另造。
- 解析讲清「为什么」对、「为什么」错，不要把选项翻译一遍。
- 试题解析必须自包含，语法、结构、词汇和搭配直接写在该题条目中，不能只靠分类笔记补充。
- 选项只保留原始选项文本，不保留频率 / 比例；答案处只写正确答案，不记录用户选错的选项。
- 试题集与分类知识笔记相互独立，不写题号回链或“关联笔记”。
- 直给结论。禁用「让我们」「总而言之」「值得注意的是」这类套话。
- 不堆叠加粗，不套多层列表，能一句话说清就别拆三层。
